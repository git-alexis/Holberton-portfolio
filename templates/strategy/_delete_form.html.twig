<form name="delete" method="post" action="{{ path('app_strategy_delete', {'id': strategy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ strategy.id) }}">
    <button class="btn btn-delete">Delete</button>
</form>
