{% extends 'base.html.twig' %}

{% block title %}Strategy{% endblock %}

{% block body %}
    <h1>Strategy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ strategy.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ strategy.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_strategy_index') }}">back to list</a>

    <a href="{{ path('app_strategy_edit', {'id': strategy.id}) }}">edit</a>

    {{ include('strategy/_delete_form.html.twig') }}
{% endblock %}
