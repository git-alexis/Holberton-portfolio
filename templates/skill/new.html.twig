{% extends 'base.html.twig' %}

{% block title %}New Skill{% endblock %}

{% block body %}
<div class="content">
    {% if user is not null %}
        <p class="utilisateur">{{ user.name ~ '.' ~ user.surname }}</p>
    {% endif %}
    <h1>Create new Skill</h1>

    {{ include('skill/_form.html.twig') }}

    <a href="{{ path('app_skill_index') }}" class="btn">back to list</a>
</div>
{% endblock %}
