{% extends 'base.html.twig' %}

{% block title %}TankSkill{% endblock %}

{% block body %}
    {% if user is not null %}
        <p class="utilisateur">{{ user.name ~ '.' ~ user.surname }}</p>
    {% endif %}
    <h1>TankSkill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tank_skill.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ tank_skill.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tank_skill_index') }}">back to list</a>

    <a href="{{ path('app_tank_skill_edit', {'id': tank_skill.id}) }}">edit</a>

    {{ include('tank_skill/_delete_form.html.twig') }}
{% endblock %}
