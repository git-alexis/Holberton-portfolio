{% extends 'base.html.twig' %}

{% block title %}TankSkill{% endblock %}

{% block body %}
    {% if user is not null %}
        <p class="utilisateur">{{ user.name ~ '.' ~ user.surname }}</p>
    {% endif %}
    <h1>TankSkill</h1>

    <div class="table-container">
    	<table class="table">
			<thead>
                <tr>
                    <th>Id</th>
                    <th>Value</th>
                </tr>
            </thead>
        	<tbody>
            	<tr>
                	<td>{{ tank_skill.id }}</td>
                	<td>{{ tank_skill.value }}</td>
            	</tr>
        	</tbody>
    	</table>
	</div>
	</br>

    <a href="{{ path('app_tank_skill_edit', {'id': tank_skill.id}) }}" class="btn btn-edit">edit</a>

    {% if is_granted('ROLE_ADMIN') %}
    	{{ include('tank_skill/_delete_form.html.twig') }}
	{% endif %}

    <a href="{{ path('app_tank_skill_index') }}" class="btn">back to list</a>
{% endblock %}
