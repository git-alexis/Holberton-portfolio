{% extends 'base.html.twig' %}

{% block title %}SkillStrategy{% endblock %}

{% block body %}
    {% if user is not null %}
        <p class="utilisateur">{{ user.name ~ '.' ~ user.surname }}</p>
    {% endif %}
    <h1>SkillStrategy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skill_strategy.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_skill_strategy_index') }}">back to list</a>

    <a href="{{ path('app_skill_strategy_edit', {'id': skill_strategy.id}) }}">edit</a>

    {{ include('skill_strategy/_delete_form.html.twig') }}
{% endblock %}
