{% extends 'base.html.twig' %}

{% block title %}SkillStrategy{% endblock %}

{% block body %}
    {% if user is not null %}
        <p class="utilisateur">{{ user.name ~ '.' ~ user.surname }}</p>
    {% endif %}
    <h1>SkillStrategy</h1>

    <div class="table-container">
    	<table class="table">
			<thead>
                <tr>
                    <th>Id</th>
                </tr>
            </thead>
        	<tbody>
            	<tr>
                	<td>{{ skill_strategy.id }}</td>
            	</tr>
        	</tbody>
    	</table>
	</div>
	</br>

    <a href="{{ path('app_skill_strategy_edit', {'id': skill_strategy.id}) }}" class="btn btn-edit">edit</a>

    {{ include('skill_strategy/_delete_form.html.twig') }}

    <a href="{{ path('app_skill_strategy_index') }}" class="btn">back to list</a>
{% endblock %}
