{% extends 'base.html.twig' %}

{% block title %}Edit SkillStrategy{% endblock %}

{% block body %}
    {% if user is not null %}
        <p class="utilisateur">{{ user.name ~ '.' ~ user.surname }}</p>
    {% endif %}
    <h1>Edit SkillStrategy</h1>

    {{ include('skill_strategy/_form.html.twig', {'button_label': 'Update'}) }}

	{% if is_granted('ROLE_ADMIN') %}
    	{{ include('skill_strategy/_delete_form.html.twig') }}
	{% endif %}

    <a href="{{ path('app_skill_strategy_index') }}" class="btn">back to list</a>
{% endblock %}
